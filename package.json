{
  "name": "my-portfolio",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "sass": "sass --watch css/custom_style.scss css/custom_style.css",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc_autosave": "tsc -w"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  }
}
