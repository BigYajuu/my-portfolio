import Provider from "../engine/state-management.ts/provider";
export class EmailDialogProvider extends Provider {
    constructor(initialState) {
        super(initialState);
    }
}
export default EmailDialogProvider;
