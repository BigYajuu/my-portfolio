import Provider from "../engine/state-management.ts/provider";
export class BackgroundProvider extends Provider {
    constructor(initialState) {
        super(initialState);
    }
}
export default BackgroundProvider;
